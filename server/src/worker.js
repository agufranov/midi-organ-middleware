const { parentPort } = require('worker_threads')

console.log(1)
// import('easymidi')
// try {
// parentPort.postMessage('from worker', easymidi.getInputs())
// } catch(e){postMessage("EEEEEEEEEE", e)}